# MongoDB Connection String
import ReviewForm from '@/components/ReviewForm';
import ReviewList from '@/components/ReviewList';
import { useReviews } from '@/hooks/useReviews';

const { reviews, isLoading, error, submitReview } = useReviews(productId);
<div className="space-y-8">
  <ReviewForm productId={productId} onSubmit={submitReview} />
  <ReviewList reviews={reviews} />
</div>

MONGODB_URI=mongodb+srv://mongodb:Rafid@cluster0.iu36527.mongodb.net/genwear?retryWrites=true&w=majority&appName=Cluster0

# OpenAI API Key
# JWT Secret Key
JWT_SECRET=genwear-secret-key-471-project-2024